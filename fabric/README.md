# fabric

Thread-mesh textile generator derived from the supplied sketch, rebuilt on plain HTML canvas with Tweakpane controls, shared paper sizing helpers, and SVG export.

Open [index.html](./index.html) in a local static server, tweak the controls, then use `Save PNG` or `Export SVG`.
