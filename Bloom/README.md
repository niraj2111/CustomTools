# Bloom

Interactive botanical path generator with:

- a draggable bezier petal template
- flower and stem arrangement modes
- Tweakpane controls for composition and styling
- SVG export built from the same vector scene model used for preview

Open [index.html](./index.html) in the folder or use the root gallery page.
